// Code generated by govalid. DO NOT EDIT.

package alpha

import (
    "errors"
	"github.com/sivchari/govalid/validation/validationhelper"
)

var (
    // ErrNilAlpha is returned when the Alpha is nil
    ErrNilAlpha = errors.New("input Alpha is nil")

    // ErrFirstNameAlphaValidation is the error returned when field FirstName is not alphabetic.
    ErrFirstNameAlphaValidation = errors.New("field FirstName must be alphabetic")
    
    // ErrLastNameAlphaValidation is the error returned when field LastName is not alphabetic.
    ErrLastNameAlphaValidation = errors.New("field LastName must be alphabetic")

    // ErrCountryCodeAlphaValidation is the error returned when field CountryCode is not alphabetic.
    ErrCountryCodeAlphaValidation = errors.New("field CountryCode must be alphabetic")
)

func ValidateAlpha(t *alpha) error {
    if t == nil {
        return ErrNilAlpha
    }

    if !validationhelper.IsValidAlpha(t.FirstName) {
        return ErrFirstNameAlphaValidation
    }

    if !validationhelper.IsValidAlpha(t.LastName) {
        return ErrLastNameAlphaValidation
    }

    if !validationhelper.IsValidAlpha(t.CountryCode) {
        return ErrCountryCodeAlphaValidation
    }

    return nil
}